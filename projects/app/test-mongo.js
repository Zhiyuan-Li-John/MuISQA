const mongoose = require("mongoose"); mongoose.connect("mongodb://localhost:27017/fastgpt").then(() => { console.log("✅ MongoDB 连接成功!"); process.exit(0); }).catch(err => { console.error("❌ MongoDB 连接失败:", err.message); process.exit(1); });
